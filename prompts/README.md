# Query Generation Prompts

- `query_generation_prompt.txt`: template for generating 4 diverse search queries from a news passage.

Usage:

1. Replace `{news_passage}` with the passage text.
2. Send the full prompt to your LLM.
3. Expect a JSON list response only.
